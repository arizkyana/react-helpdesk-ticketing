import useMain from '../../components/Layouts/Main';

import TicketDetailContainer from '../../containers/Ticket/Detail';

const TicketDetailPage = () => {
    return useMain(TicketDetailContainer);
};

export default TicketDetailPage;
